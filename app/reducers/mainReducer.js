'use strict';

const initialState = {
  someGenericVariable: true
};

export default function headerInfo(state = initialState, action) {
  switch (action.type) {

    default:
      return state;
  }
}